<%= form_for @tweet do |f| %>
	<div class="col-md-12">
		<%= f.text_area :body, placeholder: "What's going on ?", class: 'form-control txt-area' %>
		<%= f.submit 'Tweet', class: 'btn btn-info tweet-btn' %>
	</div>
<% end %>