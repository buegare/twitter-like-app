<%= form_for @tweet do |f| %>
	<div class="row" id="tweet-area-div">
		<div class="col-md-2">
			<%= image_tag("owl.png", id: 'tweet-creation-box-image') %>
		</div>
		<div class="col-md-10">
			<%= f.text_area :body, placeholder: "What's going on?", class: 'form-control', id: 'txt-area' %>
		</div>
	</div>

	<div class="row">
		<div class="col-md-3 col-md-offset-9">
			<%= f.submit 'Tweet', class: 'btn btn-info', id: 'tweet-btn' %>
			<div id="counter">140</div>
		</div>
	</div>
<% end %>

