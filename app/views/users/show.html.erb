<%= render 'show_partial' %>

	<div class="col-md-4 feed">
		<% @user.tweets.each do |tweet| %>
			<h6><strong><%= tweet.user.fullname %></strong> <%= tweet.user.username %> <%= time_ago_in_words(tweet.created_at) %> ago</h6>
				<p><%= tweet.body %></p>
		<% end %>
	</div>

</div>
